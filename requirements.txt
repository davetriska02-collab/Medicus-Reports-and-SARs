# Pinned versions — a bad upstream release must not brick fresh installs.
# Update deliberately, test, then bump here and in start_server.bat.
flask==3.1.2
pymupdf==1.26.4
striprtf==0.0.29
waitress==3.0.2
python-docx==1.2.0
extract-msg==0.55.0

# Optional — HTTPS in server mode (see INSTALL.md):
# cheroot==10.0.1
# cryptography==44.0.2
